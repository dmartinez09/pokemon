<%= form_for [current_user,caught] do |f| %>
  <% if caught.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caught.errors.count, "error") %> prohibited this caught from being saved:</h2>

      <ul>
      <% caught.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %>
    <%= f.number_field :level %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
